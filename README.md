# ART (Accelerated Raytracing Testbed)
ART is a test platform for comparing the effectiveness of different acceleration structures for ray tracing.

## Building
### Linux
Run `build.sh` with one of the following options:
```
# Debug
./build.sh # Debug is default configuration
./build.sh debug

# Release
./build.sh release
```
